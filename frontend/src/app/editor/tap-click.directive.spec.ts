import { TapClickDirective } from './tap-click.directive';

describe('TapClickDirective', () => {
  it('should create an instance', () => {
    const directive = new TapClickDirective();
    expect(directive).toBeTruthy();
  });
});
